---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install required utilities
  ansible.builtin.apt:
    name: "{{ packages_to_install }}"
    state: present